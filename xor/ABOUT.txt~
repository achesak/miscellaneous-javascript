ABOUT xor.js

xor.js defines the function Boolean.XOR() for XOR comparisons.
---------------------------------------------------------------------------------------------
USAGE

Boolean.XOR(bool1, bool2, ...)

Boolean.XOR() takes an unlimited number of arguments. It will return true if one and only one
of the arguments is true. If none or more than one are true then it will return false.